extends nav_layout

block content
  div(class='container')
    each f in foods
      +food_display(f)

mixin food_display(food)
  div(class=['col-xs-12', 'food-item-container', 'row'])
    div(class=['col-xs-4', 'food-item-img'])
      - var img = 'img/food.jpg'
      img(src=img, class='img-responsive')

    div(class=['col-xs-8', 'food-item-info'])
      div(class=['food-item-title'])
        -var userEndpoint = '/users/' + food.sellerId
        h1
          a(href=userEndpoint) #{food.name}
      div(class='food-item-description')
        p= food.description


